<!-- /*
calendar = null;


querySelect(calendar-wrapper).init(calendar => this.calendar);

<calendar-wrapper i>
<draggable-wrapper calendar={calendar}>
  <custom-event />
</draggable-wrapper>
</calendar-wrapper>

*/






/*
eventRenderCallback = args => {
  return (
    <draggable-wrapper calendar={calendar}>
      <scm-content-chip>
        <event />
      </scm-content-chip>
    </draggable-wrapper>
  )
}




calendar = null;

querySelect(calendar-wrapper).init(eventsData, eventRenderCallback, calendar => this.calendar);


<calendar-wrapper >
</calendar-wrapper>


*/ 



/*
draggable wrapper 
return (
  <Host>
    <div ref={el }>
      <slot />
    </div>
  </Host>
)

*/ 



/*

<div ref={ref} dfd dfdf>
      <scm-content-chip>
        <event />
      </scm-content-chip>
</div>
*/






/*

<draggable-wrapper>
  <list-item>
    <exapneed-evemt />
  </list-item>
</draggable-wrapper>


*/ -->